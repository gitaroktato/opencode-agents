# OpenCode Agentic Configs

## Installation

Copy over to your `~/.config/opencode` folder using:

```bash
cp -r .opencode/agents ~/.config/opencode/agents
cp -r .opencode/skills ~/.config/opencode/skills
cp ./opencode.jsonc ~/.config/opencode
```
